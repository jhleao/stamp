# Stamp website

Static public site for `stamp.leao.cloud`, including the OAuth privacy, terms,
and support pages. It intentionally has no build step, client-side JavaScript,
analytics, cookies, or forms.

```sh
wrangler pages deploy website --project-name stamp --branch main
```
